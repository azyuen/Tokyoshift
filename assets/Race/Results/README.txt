Tokyo SHIFT race result background assets.

Expected filenames:
victory_background.png
defeat_background.png
pink_slip_won_background.png
pink_slip_lost_background.png

These are loaded by RaceScene. Phaser overlays the cars, result values,
character portraits/quotes, timing stats, and action buttons.